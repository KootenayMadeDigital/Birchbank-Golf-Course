import { holeSchema } from "./schemas/hole";
import { journalSchema } from "./schemas/journal";
import { rateSchema } from "./schemas/rate";

export const schemaTypes = [holeSchema, journalSchema, rateSchema];
